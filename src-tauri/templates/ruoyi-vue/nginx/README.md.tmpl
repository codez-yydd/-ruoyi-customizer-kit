# Nginx 部署说明（{{PROJECT_NAME}}）

本目录下的 `nginx.conf` 由**若依锻造台 RuoYi Forge** 自动生成，用于前端静态托管 + 后端接口反代。

## 一、前置准备

### 1. 后端
- 后端 jar 监听端口：`{{SERVER_PORT}}`（与 `scripts/start.sh` 一致）
- 启动：`./scripts/start.sh`

### 2. 前端
- **传统 ruoyi-ui**：在前端目录执行 `npm run build:prod`，生成 `dist/`
- **Vben（web-ele）**：在前端根目录执行 `pnpm run build:ele`，产物在 `apps/web-ele/dist/`
- 两种前端生产接口前缀均为 `/prod-api`，与下方 Nginx 反代一致，无需两套配置
- 将 `dist/` 内容上传到 `/var/www/{{FRONTEND_DIR}}/`

```bash
sudo mkdir -p /var/www/{{FRONTEND_DIR}}
sudo cp -r dist/* /var/www/{{FRONTEND_DIR}}/
```

## 二、应用 Nginx 配置

```bash
# 复制配置到 Nginx 目录（按你的发行版调整路径）
sudo cp nginx.conf /etc/nginx/conf.d/{{MODULE_PREFIX}}.conf

# 校验语法
sudo nginx -t

# 重新加载
sudo nginx -s reload
```

## 三、HTTPS（如已启用）

生成配置已包含完整的 HTTPS 结构：80 端口自动 301 跳转 HTTPS，443 端口承载站点。
只需把证书放到配置声明的位置（宝塔面板风格路径）：

```bash
sudo mkdir -p /www/wwwroot/nginx/crt
# 证书链（fullchain / pem 合并后的 bundle）
sudo cp your-cert.crt /www/wwwroot/nginx/crt/{{SERVER_NAME}}_bundle.crt
# 私钥
sudo cp your-cert.key /www/wwwroot/nginx/crt/{{SERVER_NAME}}.key
```

然后重载即可：

```bash
sudo nginx -t && sudo nginx -s reload
```

> 证书放在其他位置（如 `/etc/nginx/cert/`）时，修改 `{{MODULE_PREFIX}}.conf` 中
> `ssl_certificate` / `ssl_certificate_key` 两行路径即可。

## 四、关键路径速查

| 项 | 值 |
|---|---|
| 后端端口 | `{{SERVER_PORT}}` |
| 前端目录 | `/var/www/{{FRONTEND_DIR}}` |
| 接口前缀 | `/prod-api/`（反代到后端；ruoyi-ui 与 Vben 生产环境统一） |
| 域名 | `{{SERVER_NAME}}` |
| 证书目录（HTTPS） | `/www/wwwroot/nginx/crt/` |

## 五、常见问题

- **接口 404**：确认后端 `server.servlet.context-path` 与 Nginx 的 `proxy_pass` 对齐。若依默认根路径，`/prod-api/` 反代到后端根。
- **刷新 404**：前端 Vue Router 用 history 模式时，`try_files $uri $uri/ /index.html` 必须保留。
- **上传的图片 404**：`location ^~ /prod-api/` 的 `^~` 前缀必须保留，它保证 `/prod-api/profile/xxx.png` 走后端接口而不是前端静态目录。
- **上传文件失败**：`client_max_body_size` 默认 100m，按需调大。
- **HTTPS 证书路径不符**：见第三节，改 `ssl_certificate` 两行路径后 `nginx -s reload`。
