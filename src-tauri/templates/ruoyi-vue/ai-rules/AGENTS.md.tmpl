# {{FRONTEND_TITLE}} AI 编码规范

> 本文件由 {{PROJECT_NAME}} 改造工具自动生成，用于约束 AI 辅助编程（Cursor / Copilot / Cline / Claude 等）的输出质量。可在项目演进中持续修订。

## 语言要求

- 所有回答、代码说明、Git Commit Message 默认使用简体中文。
- 除非用户明确要求英文，否则不要生成英文说明。
- 技术名词、类名、方法名、字段名、框架名保持英文原文。

---

# SOURCE FILE ENCODING

- All source files (.java, .vue, .ts, .js, .xml, .sql, .md, .yml, .properties, etc.) MUST be UTF-8.
- 禁止使用 GBK / ANSI 保存源码文件。
- 中文注释、中文字符串必须正常显示。
- 禁止提交乱码内容，例如：`鍐呭規爣绛` / `????` / `口口口`。
- 修改文件前，如果发现原文件存在编码异常，必须优先提示并修复编码问题。

---

# CODE COMMENT RULES

- 关键业务逻辑必须写清楚中文注释。
- 注释重点解释“为什么这样做”，不要只重复代码表面含义。
- 复杂 if/else、循环、状态流转、权限判断、金额、库存、订单、时间处理等核心逻辑必须补充注释。
- 新增类、核心方法、公共方法必须添加清晰注释。
- DTO、VO、Form、Entity 中的重要字段必须补充字段含义注释。
- 方法逻辑复杂时，应在方法开头说明整体处理流程。
- 不允许生成无意义注释，例如 `// 设置名称` / `// 获取列表` / `// TODO` / `// 处理数据`。

---

# METHOD FLOW COMMENT RULES

- 方法内部调用其他方法时，必须使用中文注释说明当前步骤的业务目的。
- 注释重点说明：为什么调用 / 当前步骤解决什么问题 / 在整体流程中的作用。
- 不允许只写“调用某方法”这种无意义注释。
- 多步骤业务流程必须按阶段增加注释，提高代码可读性。

禁止：

```java
// 查询用户
queryUser()

// 保存订单
saveOrder()
```

推荐：

```java
// 查询用户基础信息，用于后续权限校验
queryUser()

// 保存订单主记录，生成系统订单号
saveOrder()

// 扣减库存，避免超卖
deductStock()

// 调用支付中心创建支付流水
createPayment()
```

---

# VARIABLE COMMENT RULES

- 核心业务变量必须说明业务含义与用途。
- 注释必须解释变量存在意义，而不是翻译变量名。
- 缓存变量、状态变量、金额变量、权限变量、时间变量必须补充说明。

禁止：

```java
// 用户列表
List<User> userList
```

推荐：

```java
// 当前机构下可见用户列表，用于数据权限过滤
List<User> visibleUserList
```

---

# METHOD COMMENT RULES

- 公共方法、核心业务方法必须使用 JavaDoc 注释。
- 注释必须说明：方法业务作用 / 参数业务含义 / 返回值含义 / 是否存在副作用。
- 方法逻辑超过 30 行时，必须在方法开头说明整体执行流程。

推荐：

```java
/**
 * 创建采购订单并初始化审批流程
 *
 * 处理流程：
 * 1. 校验供应商状态
 * 2. 校验采购商品合法性
 * 3. 创建采购订单
 * 4. 初始化审批记录
 * 5. 推送待审批消息
 *
 * @param createForm 采购订单参数
 * @return 采购订单ID
 */
```

---

# COMMENT QUALITY RULES

- 注释必须提供“业务信息增量”，禁止生成“翻译代码式注释”。
- 注释必须优先解释：为什么这样实现 / 为什么不能直接处理 / 为什么需要特殊判断 / 为什么需要缓存/事务/锁/补偿。

禁止：

```java
// 判断是否为空
if (obj != null)
```

推荐：

```java
// 第三方接口可能返回空对象，这里避免出现空指针导致任务中断
if (paymentResult != null)
```

---

# NAMING RULES

- 变量名、方法名、类名必须语义清晰。
- 禁止使用无意义命名：`a` / `b` / `temp` / `data1` / `list1` / `obj`。
- 临时变量也必须体现业务含义。
- 布尔变量必须使用明确语义：`isEnabled` / `hasPermission` / `canSubmit`。
- 方法名必须体现动作与业务对象：`buildUserTree` / `validateOrderStatus` / `queryDevicePage`。

---

# JAVA / SPRING BOOT RULES

- Java 默认按 JDK 17 编写。
- 优先使用稳定、易维护、易理解的实现方式，不要为了炫技使用过度复杂写法。
- Controller 层仅负责：参数接收、权限控制、结果返回。
- Service 层负责业务逻辑；Mapper 层仅负责数据访问。
- 新增接口必须考虑：参数校验、异常处理、权限控制、日志记录。
- 涉及数据库批量操作时：优先批量查询 / 批量插入 / 批量更新，禁止循环内频繁查库。
- 不要随意修改已有业务逻辑；不要删除已有字段、方法、注释、业务分支，除非用户明确要求。
- 优先保持原有代码结构与业务行为一致。

---

{{SNOWFLAKE_ID_SECTION}}
# BUSINESS STABILITY RULES

- 禁止擅自修改已有业务状态流转。
- 禁止改变原有接口返回结构。
- 禁止修改已有数据库字段语义。
- 涉及以下逻辑时必须优先保证兼容性：金额 / 库存 / 支付 / 审批 / 状态机 / 权限 / 数据同步。
- 如果发现原逻辑可能存在历史兼容依赖，必须先提示风险后再修改。
- 不允许为了“优化代码”擅自改变业务行为。
- 如果用户未明确要求“重构”，则优先保持原有代码结构与写法。

---

# DTO / VO / ENTITY RULES

- Entity 仅用于数据库映射。
- DTO 用于接口参数传输。
- VO 用于页面结果返回。
- 禁止直接返回 Entity 给前端；禁止接口参数直接使用 Entity 接收。

---

# MAPPER / XML SQL RULES

- Mapper XML 中复杂 SQL 必须添加中文注释。
- 多表关联、子查询、动态 SQL、CASE WHEN、批量更新等逻辑必须说明用途。
- 注释重点说明：为什么这样查询 / 为什么这样关联 / 为什么这样排序 / 为什么需要 distinct/group by。
- 动态 SQL (`<if>`、`<choose>`、`<foreach>`) 必须说明业务条件。
- SQL 字段别名必须具备业务语义，禁止使用无意义别名（`t1` / `aa` / `bb`）。
- XML 中超过 15 行 SQL 建议按模块换行对齐。

---

# SQL RULES

- SQL 必须考虑：索引 / 数据量 / 锁表风险 / 执行效率。
- 修改表结构时必须补充字段注释。
- UPDATE / DELETE 必须带明确 WHERE 条件。
- 涉及生产数据操作时：优先给出可验证方案，优先给出可回滚方案。
- 存储过程、批量脚本必须保持可读性，并补充关键逻辑注释。

---

# LOG RULES

- 关键业务操作必须记录日志。
- 日志必须包含关键业务参数：用户ID / 订单号 / 设备编号 / 请求参数摘要。
- 禁止打印敏感信息：密码 / Token / 身份证 / 完整手机号。
- catch 异常时必须输出异常堆栈，禁止仅打印 `e.getMessage()`。
- 高频循环中禁止大量打印日志。
- 日志内容必须具备业务可读性。

---

# EXCEPTION RULES

- 禁止直接抛出 RuntimeException；业务异常必须使用统一 BusinessException。
- 异常信息必须具备业务可读性。
- Controller 层不要大量 try/catch，异常统一交给全局异常处理。
- 参数校验失败必须返回明确提示。

---

# PAGE QUERY RULES

- 所有列表接口必须支持分页，禁止无分页查询大表。
- 分页参数必须包含 pageNum / pageSize。
- 查询条件必须优先考虑索引字段。

---

# ENUM RULES

- 状态值、类型值、业务标识禁止直接写 magic number，必须使用 enum 管理。

禁止：

```java
if (status == 1)
```

推荐：

```java
if (status == OrderStatusEnum.PAID.getCode())
```

---

# NULL CHECK RULES

- 外部接口返回值必须判空。
- 数据库查询结果必须判空。
- 集合使用前必须判断 empty。
- 禁止连续调用可能为空对象。

---

# API RESPONSE RULES

- 接口返回必须统一结构；返回字段必须保持稳定，禁止随意修改字段名。
- 新增字段必须考虑前端兼容性。
- 错误码必须统一管理。

---

# DEPENDENCY MANAGEMENT RULES

- Maven 依赖版本必须锁定，禁止使用 SNAPSHOT 版本（除内部正在研发的模块）。
- 新增依赖前评估：是否真的必要 / 是否有更轻量的替代 / 是否会引入安全漏洞。
- 定期检查依赖是否有已知 CVE，及时升级。
- 优先使用 Spring Boot Starter 管理的版本，避免手动指定版本号造成冲突。
- 禁止引入重复功能的库（例如同时引入 fastjson 和 jackson 做同一件事）。

---

# IDEMPOTENCY RULES

- 涉及支付、下单、扣库存、发券等关键写操作，接口必须保证幂等。
- 幂等实现优先级：唯一索引 > 分布式锁（Redis）> 状态机校验 > token 机制。
- 幂等 key 设计必须包含业务维度（如 `pay:order:{orderId}`），避免不同业务互相干扰。
- 前端重复点击、网络重试、回调重放都必须能正确处理。
- 幂等失败的返回必须明确（"请勿重复提交"），而不是通用错误。

---

# CACHE RULES

- 缓存 key 必须有统一前缀和命名规范（如 `{业务}:{实体}:{id}`），禁止无序命名。
- 所有缓存必须设置过期时间，禁止写入永久缓存（除非有明确的淘汰机制）。
- 缓存与数据库一致性：写操作优先"先更新库再删缓存"，避免脏数据。
- 禁止缓存大对象（建议单 key 不超过 10KB），大对象考虑分片或压缩。
- 缓存穿透/击穿/雪崩必须考虑：空值缓存 / 互斥锁 / 过期时间随机化。
- 关键业务数据禁止强依赖缓存，缓存失效时必须有降级到数据库的能力。

---

# CONCURRENCY RULES

- 共享可变状态必须考虑并发安全，优先使用无锁方案（不可变对象 / CAS / 单线程化）。
- 必须使用锁时，优先分布式锁（Redisson），锁粒度尽量小，持有时间尽量短。
- 锁必须有超时时间（避免死锁），必须有 owner 标识（避免误释放）。
- `@Transactional` 事务边界必须清晰：不要在事务内调用远程接口（耗时、可能回滚失败）。
- 大事务必须拆分，长事务会占用数据库连接、导致主从延迟。
- 涉及多个资源（DB + MQ + 缓存）的操作，必须明确最终一致性方案，避免部分成功。

---

# TEST RULES

- 核心业务逻辑（金额计算、状态流转、权限判断、库存扣减）必须有单元测试。
- 单元测试必须覆盖：正常路径 + 边界值 + 异常场景。
- 禁止写只验证"不报错"的无效测试，必须有明确断言。
- 涉及金额、时间的测试，禁止依赖系统当前时间或固定货币格式。
- Mock 必须基于接口，不要 Mock 具体实现类。
- 测试方法名必须表达意图：`should_return_error_when_stock_not_enough`。

---

# DEPLOYMENT RULES

- 环境相关配置（数据库、Redis、第三方密钥）必须外部化，禁止硬编码到代码或 yml 明文。
- 生产环境日志级别默认 INFO，禁止 DEBUG 上生产（会产生大量日志影响性能）。
- 敏感信息（密码、密钥、证书）通过环境变量或配置中心注入，禁止提交到 git。
- 接口必须有超时配置（连接超时 + 读超时），禁止使用无限超时。
- 线程池必须有明确的拒绝策略和队列大小，禁止使用无界队列。

---

# VUE 3 / TYPESCRIPT RULES

- 默认使用 Vue 3 + TypeScript + Element Plus。
- 不要省略已有字段、表单项、表格列、接口参数。
- 页面逻辑必须保持结构清晰，避免单文件堆积过多逻辑。
- TypeScript 类型必须明确，尽量避免使用 any。
- 表单字段、表格列、接口字段必须保留业务注释。
- 页面必须处理：响应式 / loading 状态 / 空数据 / 异常提示。
- 删除、提交、导入等危险操作必须二次确认。

---

# SECURITY RULES

- 禁止拼接 SQL；禁止信任前端传入权限字段。
- 上传文件必须校验类型与大小；导出接口必须考虑数据权限。
- 接口必须考虑越权问题（水平越权 / 垂直越权）。
- 密码、身份证、手机号等敏感字段必须脱敏存储或展示。

---

# AI SAFETY RULES

- 未经明确要求，不允许删除已有业务逻辑、字段、接口、SQL、配置、注释。
- 修改代码时必须优先兼容已有逻辑。
- 如果发现逻辑可能影响旧功能，必须先提示风险。
- AI 生成代码时优先保证可维护性。
- 不允许为了“优化代码”擅自改变业务行为。
- 如果用户未明确要求“重构”，则优先保持原有代码结构与写法。

---

# GIT COMMIT MESSAGE RULES

IMPORTANT: These rules apply to the "Generate with AI" in the Git panel.

- Git commit message 必须使用简体中文，不要使用英文描述。
- 使用 Conventional Commits 格式：`type:中文描述`（冒号后不要加空格）。

可用 type：

- `feat:` 新功能
- `fix:` 修补错误
- `docs:` 文档改变
- `style:` 代码格式改变
- `refactor:` 已有功能重构
- `perf:` 性能优化
- `test:` 增加测试
- `build:` 改变构建流程
- `ci:` 改变持续集成配置
- `chore:` 辅助工具的变动

示例：

- `feat:完善用户登录逻辑`
- `fix:解决诗词详情页加载异常`
- `docs:更新README文件`
- `refactor:优化任务管理页面结构`
- `perf:优化批量查询性能`
